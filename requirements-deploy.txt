# Deployment-only dependencies (no docling, no scraping libs)
# These are what the Docker container needs to run the app

# API + LLM
openai>=1.30.0
fastapi>=0.111.0
uvicorn>=0.29.0
pydantic-settings>=2.2.0

# Vector store (with fastembed for local embeddings)
qdrant-client[fastembed]>=1.9.0

# Frontend
streamlit>=1.35.0

# Config
pyyaml>=6.0.0
python-dotenv>=1.0.0
requests>=2.31.0
